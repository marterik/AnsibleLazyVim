webservers:
  hosts:
    server1:
      ansible_host: 192.168.122.113
  vars:
    ansible_user: auser
    ansible_ssh_pass: apassword
    ansible_become_pass: "{{ ansible_ssh_pass }}"
    nvim_config_dest: /home/erik/.config/nvim
